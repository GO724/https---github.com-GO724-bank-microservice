package card

import "context"

func (c *cardService) Delete(ctx context.Context, id uint) error {
	//b.personRepo.Get()
	// if err
	/// some logic
	//b.cardRepo.Delete()

	//return b.bankRepo.Delete(bic)
	return nil
}
