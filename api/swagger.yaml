swagger: '2.0'
info:
  description: |+
    Sample OpenAPI2.0 spec for demo microservice bcard:
    Check valid paycard (exist & not expiried)
    CRUD Card, Person, Bank & check valid

  version: 1.0.0
  title: bcard_microservice_api
  # put the contact info for your development or API team
  contact:
    email: nba.linx@gmail.com

  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html

# tags are used for organizing operations
tags:
- name: admins
  description: Secured Admin-only calls
- name: service
  description: Service data access

#Create
#Route: POST /classes
#Effect on Database: Adds the class provided in the request body to the database
#Response Body: { "class": The Newly-Created Class }
#Success Response Code: 201

#Read (All Classes)
#Route: GET /classes
#Effect on Database: None
#Response Body: { "classes": [ Array of All Saved Classess ] }
#Success Response Code: 200

#Read (One Class)
#Route: GET /classes/:id
#Effect on Database: None
#Response Body: { "class": The class with the specified ID }
#Success Response Code: 200

#Update
#Route: PUT /classes/:id
#Effect on Database: Updates the class with the specified ID to have the class information provided in the request body
#Response Body: { "class": The updated class now saved in the database }
#Success Response Code: 200

#Delete
#Route: DELETE /classes/:id
#Effect on Database: Removes the class with the specified ID from the database
#Response Body: None
#Success Response Code: 204

paths:
  /persons:
    post:  #Create
      tags:
      - admins
      summary: adds an person item
      operationId: addPerson
      description: Adds an person to the system
      consumes:
      - application/json
      produces:
      - application/json
      parameters:
      - in: body
        name: Person
        description: Person to add
        schema:
          $ref: '#/definitions/Person'
      responses:
        201:
          description: person created
        400:
          description: invalid input, object invalid
        409:
          description: person already exists

    get:  #(Read all selected)
      tags:
      - admins
      summary: searches persons
      #operationid: searchPersons
      description: |
        By passing in the appropriate options, you can search for
        persons in the system
      produces:
      - application/json
      parameters:
      - in: query
        name: searchString
        description: pass an optional search string for looking up person
        required: false
        type: string
      - in: query
        name: skip
        description: number of records to skip for pagination
        type: integer
        format: int32
        minimum: 0
      - in: query
        name: limit
        description: maximum number of records to return
        type: integer
        format: int32
        minimum: 0
        maximum: 50
      responses:
        200:
          description: search results matching criteria
          schema:
            type: array
            items:
              $ref: '#/definitions/Person'
        400:
          description: bad input parameter

  /persons/{inn}:  #(Read, Delete)        
    get:      #(Read)
      tags:
      - admins
      summary: get an person item
      operationId: getPerson
      description: Gets an person from the system
      produces:
      - application/json
      parameters:
      - name: inn
        type: integer
        in: path
        required: true
        description: get person
      responses:
        201:
          description: person found
        400:
          description: invalid input, object invalid
        409:
          description: person not exists
    delete:   #(Delete)
      tags:
      - admins
      summary: remove an person item
      operationId: delPerson
      description: Remove an person from the system
      produces:
      - application/json
      parameters:
      - name: inn
        type: integer
        in: path
        required: true
        description: Person to remove
      responses:
        204:  #201?
          description: person deleted
        400:
          description: invalid input, object invalid
        409:
          description: person not exists

  /banks:
    post:  #Create
      tags:
      - admins
      summary: adds an bank item
      operationId: addbank
      description: Adds an bank to the system
      consumes:
      - application/json
      produces:
      - application/json
      parameters:
      - in: body
        name: bank
        description: bank to add
        schema:
          $ref: '#/definitions/Bank'
      responses:
        201:
          description: bank created
        400:
          description: invalid input, object invalid
        409:
          description: bank already exists

    get:  #(Read all selected)
      tags:
      - admins
      summary: searches banks
      description: |
        By passing in the appropriate options, you can search for
        banks in the system
      produces:
      - application/json
      parameters:
      - in: query
        name: searchString
        description: pass an optional search string for looking up bank
        required: false
        type: string
      - in: query
        name: skip
        description: number of records to skip for pagination
        type: integer
        format: int32
        minimum: 0
      - in: query
        name: limit
        description: maximum number of records to return
        type: integer
        format: int32
        minimum: 0
        maximum: 50
      responses:
        200:
          description: search results matching criteria
          schema:
            type: array
            items:
              $ref: '#/definitions/Bank'
        400:
          description: bad input parameter

  /banks/{inn}:  #(Read, Delete)        
    get:      #(Read)
      tags:
      - admins
      summary: get an bank item
      operationId: getBank
      description: Gets an bank from the system
      produces:
      - application/json
      parameters:
      - name: inn
        type: integer
        in: path
        required: true
        description: get bank
      responses:
        201:
          description: bank found
        400:
          description: invalid input, object invalid
        409:
          description: bank not exists
    delete:   #(Delete)
      tags:
      - admins
      summary: remove an bank item
      operationId: delBank
      description: Remove an bank from the system
      produces:
      - application/json
      parameters:
      - name: inn
        type: integer
        in: path
        required: true
        description: bank to remove
      responses:
        204:  #201?
          description: bank deleted
        400:
          description: invalid input, object invalid
        409:
          description: bank not exists
          
  /cards:
    post:  #Create
      tags:
      - admins
      summary: adds an card item
      operationId: addCard
      description: Adds an card to the system
      consumes:
      - application/json
      produces:
      - application/json
      parameters:
      - in: body
        name: card
        description: card to add
        schema:
          $ref: '#/definitions/Card'
      responses:
        201:
          description: card created
        400:
          description: invalid input, object invalid
        409:
          description: card already exists

    get:  #(Read all selected)
      tags:
      - admins
      summary: searches cards
      description: |
        By passing in the appropriate options, you can search for
        cards in the system
      produces:
      - application/json
      parameters:
      - in: query
        name: searchString
        description: pass an optional search string for looking up card
        required: false
        type: string
      - in: query
        name: skip
        description: number of records to skip for pagination
        type: integer
        format: int32
        minimum: 0
      - in: query
        name: limit
        description: maximum number of records to return
        type: integer
        format: int32
        minimum: 0
        maximum: 50
      responses:
        200:
          description: search results matching criteria
          schema:
            type: array
            items:
              $ref: '#/definitions/Card'
        400:
          description: bad input parameter

  /cards/{inn}:  #(Read, Delete)        
    get:      #(Read)
      tags:
      - admins
      summary: get an card item
      operationId: getCard
      description: Gets an card from the system
      produces:
      - application/json
      parameters:
      - name: inn
        type: integer
        in: path
        required: true
        description: get card
      responses:
        201:
          description: card found
        400:
          description: invalid input, object invalid
        409:
          description: card not exists
    delete:   #(Delete)
      tags:
      - admins
      summary: remove an card item
      operationId: delСard
      description: Remove an card from the system
      produces:
      - application/json
      parameters:
      - name: inn
        type: integer
        in: path
        required: true
        description: card to remove
      responses:
        204:  #201?
          description: card deleted
        400:
          description: invalid input, object invalid
        409:
          description: card not exists
  /card/validator/{id}:
    get:
      tags:
      - admins
      - service
      summary: check card
      operationId: isValid
      description: |
        Check card status: if card.exists & card.valid>now() > valid = true
      produces:
      - application/json
      parameters:
      - name: id
        type: integer
        in: path
        required: true
        description: get card status
      responses:
        200:
          description: search results matching criteria
          schema:
            type: array
            items:
              $ref: '#/definitions/Card'
        400:
          description: bad input parameter
          
definitions:
  Person:
    type: object
    required:
    - inn
    - name
    properties:
      inn:
        type: integer
        format: uint64
        example: 000000000000
      name:
        type: string
        example: John Smith
    description: Sample person model
  Bank:
    type: object
    required:
    - bic
    - name
    properties:
      bic:
        type: integer
        format: uint32
        example: 000000000
      name:
        type: string
        example: Bank of Albania
    description: Sample bank model
  Card:
    type: object
    required:
    - id
    - bic
    - name
    - valid
    properties:
      id:
        type: integer
        format: uint64
        example: 000000000000000000000
      bic:
        type: integer
        format: uint32
        example: 000000000
      name:
        type: string
        example: Bank of Albania
      valid:
        type: string
        format: date-time
        example: '2016-08-29T09:12:33.001Z'
    description: Sample card model
  
# Added by API Auto Mocking Plugin
host: virtserver.swaggerhub.com
basePath: /go9130416421/bcard/1.0.0
schemes:
 - https